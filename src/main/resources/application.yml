spring:
  datasource:
    url: jdbc:mysql://mysql_database:3306/accenture
    username: root
    password: 5610
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  logging:
    level:
      org:
        springframework:
          web: debug

